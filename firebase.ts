
// Firebase has been removed in favor of a Python/MongoDB backend.
// This file is kept as a placeholder to prevent import errors during migration
// but should ideally be deleted once all references are removed.

export const auth = null;
export const db = null;
